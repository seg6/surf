# CI has no physical sound card. Keep CPAL's real ALSA playback path, routed
# through PulseAudio to a clocked null sink configured by the test job.
</usr/share/alsa/alsa.conf>

pcm.!default {
    type pulse
}

ctl.!default {
    type pulse
}
